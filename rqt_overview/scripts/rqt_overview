import os
import sys

import rospkg
r = rospkg.RosPack()
p = r.get_path('rqt_gui')
sys.path.append(os.path.join(p, 'src'))

from rqt_gui.main import Main
main = Main()
sys.exit(main.main(sys.argv, standalone='rqt_overview'))
